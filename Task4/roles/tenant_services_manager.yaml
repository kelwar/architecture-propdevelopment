apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: tenant_services
  name: tenant_services_manager
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list", "watch", "create", "update", "patch"]